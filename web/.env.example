# For local dev the Next.js proxy forwards /api to the FastAPI backend, so you
# don't need this. For a deployed frontend (Vercel), set it to your backend URL:
# NEXT_PUBLIC_API_BASE=https://meridian-api.onrender.com
NEXT_PUBLIC_API_BASE=
